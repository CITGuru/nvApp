{
  "name": "vapp",
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    },
    {
      "url": "https://github.com/heroku/heroku-buildpack-static.git"
    }
  ],
  "scripts": {
    "postdeploy": "npm install -g @vue/cli-service && npm run build",
    "heroku-postbuild": "npm run build"
  }
}
